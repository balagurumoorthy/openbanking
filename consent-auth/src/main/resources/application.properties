quarkus.http.port=8081

quarkus.datasource.db-kind=${OB_DB_KIND:h2}
quarkus.datasource.jdbc.url=${OB_DB_URL:jdbc:h2:mem:consent;DB_CLOSE_DELAY=-1}
quarkus.datasource.username=${OB_DB_USER:sa}
quarkus.datasource.password=${OB_DB_PASSWORD:}
quarkus.hibernate-orm.database.generation=none
quarkus.hibernate-orm.physical-naming-strategy=org.hibernate.boot.model.naming.CamelCaseToUnderscoresNamingStrategy
quarkus.flyway.migrate-at-start=true

# JWT issuing keys (private signs tokens, public published at /jwks via mp.jwt verify key)
smallrye.jwt.sign.key.location=${OB_JWT_PRIVATE_KEY:jwt-signing-pkcs8.key}
mp.jwt.verify.publickey.location=${OB_JWT_PUBLIC_KEY:jwt-signing.pub}
mp.jwt.verify.issuer=https://consent-auth.balabank.local

# Access token lifetime (seconds)
ob.token.ttl=3600
